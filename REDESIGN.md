# AgentMux 界面重构设计

## 问题分析

当前设计的问题：
1. **页面太细碎**：9 个独立页面（概览、提供方、监听、事件、回复、会话、压缩、插件、设置）
2. **缺少项目概念**：没有"项目"这个核心概念
3. **菜单结构不合理**：按照需求文档的菜单来设计，而不是按照用户使用流程

## 新设计方案

### 参考设计

参考 Qoder/Codex 等现代开发工具的设计模式：
- **左侧边栏**：项目列表 + 会话列表（类似 IDE 的文件树）
- **中间主区域**：会话内容（类似聊天窗口）
- **右侧面板**：上下文/配置（可折叠）
- **顶部工具栏**：全局操作（创建项目、设置等）

### 核心概念

1. **项目（Project）**
   - 名称（自定义）
   - 工作目录（Agent 可见范围）
   - Agent CLI 路径
   - 钉钉 CLI 路径
   - 配置（回复开关、超时、字数上限等）

2. **会话（Session）**
   - 属于某个项目
   - 对应一个钉钉会话（群聊或单聊）
   - 包含消息历史
   - 关联 Agent 会话（sessionId）

3. **消息（Message）**
   - 属于某个会话
   - 发送人、时间、内容
   - 方向（用户/Agent）

### 界面布局

```
┌─────────────────────────────────────────────────────────────┐
│  [AgentMux]  [创建项目]  [设置]                              │  <- 顶部工具栏
├──────────────┬──────────────────────────────┬───────────────┤
│              │                              │               │
│  项目列表    │    会话内容                   │   上下文面板  │
│              │                              │               │
│  ▼ 项目1     │  [用户消息]                  │  [上下文]     │
│    会话1     │  [Agent回复]                 │  最近50条     │
│    会话2     │  [用户消息]                  │  8000字符     │
│    会话3     │  [Agent回复]                 │               │
│              │                              │  [压缩]       │
│  ▼ 项目2     │                              │               │
│    会话1     │                              │  [配置]       │
│    会话2     │                              │  回复: 开     │
│              │                              │  超时: 120s   │
│              │                              │               │
├──────────────┴──────────────────────────────┴───────────────┤
│  状态栏: 监听中 | 已处理 123 条 | 已回复 45 条              │
└─────────────────────────────────────────────────────────────┘
```

### 用户流程

1. **首次使用**
   - 启动应用，显示空状态
   - 点击"创建项目"
   - 填写项目名称、选择工作目录、Agent CLI、钉钉 CLI
   - 项目创建完成

2. **日常使用**
   - 左侧选择项目
   - 项目下自动列出所有会话（从钉钉拉取）
   - 点击会话，中间显示消息历史
   - 右侧显示上下文和配置
   - 可以手动压缩上下文

3. **监听管理**
   - 顶部工具栏有"启动监听"/"停止监听"按钮
   - 状态栏显示监听状态
   - 新消息自动出现在对应会话中

### 技术实现

#### 后端改动

1. **Project 模型**
```rust
pub struct Project {
    pub id: String,
    pub name: String,
    pub work_dir: String,
    pub agent_cli_path: String,
    pub dingtalk_cli_path: String,
    pub config: ProjectConfig,
    pub created_at: String,
    pub updated_at: String,
}
```

2. **新增 API**
- `create_project` - 创建项目
- `list_projects` - 列出项目
- `get_project` - 获取项目详情
- `update_project` - 更新项目
- `delete_project` - 删除项目
- `list_sessions` - 列出项目的会话
- `get_session_messages` - 获取会话消息
- `compress_context` - 压缩上下文

#### 前端改动

1. **页面结构**
- 删除原有的 9 个独立页面
- 实现新的三栏布局
- 实现项目管理和会话管理

2. **组件**
- `ProjectList` - 项目列表
- `SessionList` - 会话列表
- `MessageView` - 消息视图
- `ContextPanel` - 上下文面板
- `ProjectDialog` - 创建/编辑项目对话框

### 迁移计划

1. **保留后端核心逻辑**
   - providers、orchestrator、storage、config 模块保留
   - 新增 project 模块

2. **重构前端**
   - 删除旧的 9 个页面
   - 实现新的三栏布局
   - 实现项目概念

3. **数据迁移**
   - 现有配置迁移到新项目结构
   - 现有会话关联到项目

### 时间估算

- 后端改动：2-3 小时
- 前端重构：4-6 小时
- 测试验证：1-2 小时
- 总计：7-11 小时

### 下一步

1. 确认设计方案
2. 实现后端 Project 模型和 API
3. 重构前端界面
4. 测试验证
5. 数据迁移
