# agentmux 需求待决问题台账

**状态**：79 条中 **73 条已解决**（66 条按建议执行 + 7 条实测解决），**6 条仍需长期观察或非实测项**。

**回填口径**：已采纳项的结论不是"当作已知"，而是"先按此口径执行"——它们是 `需求明细.js` 里的业务规则（来源标 `自定义` 或 `代码实测`），可被推翻，推翻时需同步改规则与本表。

**编号**：D-01~D-79 沿用首次抽取的顺序，功能编码 → 编号的映射不变。

---

## 一、仍待处理（6 条，不能靠单次实测解决）

这些项要么需要长期观察，要么是视觉设计，要么依赖实现期确认。**在拿到结论前，对应功能的实现保持未定**。

| 编号 | 功能 | 类别 | 问题 | 建议方向 |
|---|---|---|---|---|
| D-11 | A2.1.3 AMX-203 | 视觉设计 | 三态文案与配色未定 | 待 A2.1 视觉确认；要求未安装与未登录一眼可辨 |
| D-23 | A3.1.2 AMX-302 | 需用户配合 | 单聊的 conversation_id 形态未验证 | 需从另一账号发单聊消息验证会话标识格式 |
| D-33 | A3.2.3 AMX-309 | 需长期运行 | 断连重连与保活二选一未决策 | 建议先实测空闲到点后 bus 的实际行为，再在重连与保活中定；决策前不实现 |
| D-59 | A7.1.2 AMX-702 | 需长期使用 | 触发阈值的具体数值未定 | 建议先在真实群上记录长度与回答质量的关系再定值，不拍脑袋 |
| D-75 | A9.1.5 AMX-905 | 需长期使用 | 关闭上下文后是否影响回答质量需实测 | 建议在设置项旁说明「关闭后回答可能缺少上下文」 |
| D-76 | A9.1.6 AMX-906 | 实现期逐项确认 | 哪些改动必须重启应用未定 | 需在实现时逐项确认，并在本页如实标注 |

---

## 二、已实测解决（7 条，2026-09-19）

| 编号 | 功能 | 实测结论 | 落到明细的规则 |
|---|---|---|---|
| D-05 | A2.1.1 AMX-201 | `dws version` 输出 `v1.0.62 (commit, date)`，可解析 | IM 侧 version 获取 |
| D-08 | A2.1.2 AMX-202 | `dws auth status` 返回 JSON 含 authenticated / token_valid / refresh_token_valid / expires_at / user_name | Agent 登录态探测 |
| D-09 | A2.1.2 AMX-202 | 同 D-05，`dws version` 可用 | Agent 侧 version 获取 |
| D-22 | A3.1.2 AMX-302 | `all-direct` 模式订阅成功，event_key=`user_im_message_receive_o2o_all` | 单聊监听可用 |
| D-24 | A3.1.3 AMX-303 | 两个监听进程可并发共存，共享同一 bus_pid，各自独立 subscribe_id | 多监听可并发 |
| D-25 | A3.1.3 AMX-303 | 多监听可并发，无需降级方案 | 无需降级方案 |
| D-56 | A6.2.3 AMX-606 | `--limit` 无服务端硬性上限报错，单页最多约 50 条；获取更多需用 `--page-all` | 远端历史条数上限 |

---

## 三、已采纳并归档（66 条规则，2026-09-19）

| 编号 | 功能 | 采纳的结论 | 落到明细的规则 |
|---|---|---|---|
| D-01 | A1.1.1 AMX-101 | v1 只放「提供方 + 监听 + 回复」三元 | 总览展示范围 |
| D-02 | A1.1.2 AMX-102 | 包含全部已落盘事件，回复结果单独一列 | 统计口径 |
| D-03 | A1.1.3 AMX-103 | 串行单进程 | 全量重检串行 |
| D-04 | A1.1.4 AMX-104 | 上限 200 条，并随归档落盘 | 异常保留上限 |
| D-06 | A2.1.1 AMX-201 | 不缓存，检测为显式动作 | 检测结果不缓存 |
| D-07 | A2.1.1 AMX-201 | 10s | 检测超时 |
| D-10 | A2.1.2 AMX-202 | 由适配器静态声明 | 续接能力由适配器声明 |
| D-12 | A2.1.4 AMX-204 | 复用进行中的请求 | 检测去重 |
| D-13 | A2.1.4 AMX-204 | 全局同时最多 1 个检测进程 | 检测并发上限 |
| D-14 | A2.1.5 AMX-205 | 只保留最近一次 | 诊断记录保留 |
| D-15 | A2.1.5 AMX-205 | 至少掩码 token / cookie / 手机号 | 敏感信息掩码 |
| D-16 | A2.2.1 AMX-206 | 不自动重启，UI 明示 | 改路径不自动重启监听 |
| D-17 | A2.2.2 AMX-207 | 工作目录未变则保留会话 | 换 CLI 不影响既有会话 |
| D-18 | A2.2.3 AMX-208 | 允许选中但显式标注不可用 | 未就绪 Agent 的选中处理 |
| D-19 | A2.2.3 AMX-208 | 保留记录并标记失效 | 切换 Agent 后的旧会话 |
| D-20 | A2.2.4 AMX-209 | v1 只允许只读白名单 | v1 只允许只读工具白名单 |
| D-21 | A3.1.1 AMX-301 | 已落盘事件继续处理完 | 关闭监听不丢在途事件 |
| D-26 | A3.1.4 AMX-304 | 首屏 invoke 拉快照，之后 Channel 推增量 | 状态刷新方式 |
| D-27 | A3.1.5 AMX-305 | 仅在 killed/stuck 时补退订 | 补退订的触发条件 |
| D-28 | A3.1.6 AMX-306 | 30s | 就绪超时 |
| D-29 | A3.1.6 AMX-306 | 设上限并记录丢弃数 | 就绪前缓冲上限 |
| D-30 | A3.2.1 AMX-307 | Tauri v2 Channel 推流 | 推流通道 |
| D-31 | A3.2.1 AMX-307 | 5000 行 | 日志显示缓冲上限 |
| D-32 | A3.2.2 AMX-308 | 固定为实测序列 | 退避序列 v1 固定 |
| D-34 | A3.2.3 AMX-309 | 在最近异常与监听日志明示 | 恢复期丢消息提示 |
| D-35 | A4.1.1 AMX-401 | 先落盘再推流 | 事件流与归档同源 |
| D-36 | A4.1.2 AMX-402 | v1 只用昵称 + 身份 id | 不展示真实姓名 |
| D-37 | A4.1.3 AMX-403 | 随归档保留 | 原始载荷保留期 |
| D-38 | A4.2.1 AMX-404 | 31 天 | 查询跨度上限 |
| D-39 | A4.2.2 AMX-405 | v1 只 ndjson | 导出格式 |
| D-40 | A4.2.3 AMX-406 | 仅连续出现时提示 | 畸形事件告警策略 |
| D-41 | A4.2.4 AMX-407 | 量级可控时不淘汰 | 去重集合不淘汰 |
| D-42 | A5.1.1 AMX-501 | 仍在途的按旧值处理完 | 切换总开关不丢弃在途 |
| D-43 | A5.1.2 AMX-502 | 设上限，超限丢弃最旧 | 队列上限与拒绝策略 |
| D-44 | A5.1.3 AMX-503 | 随归档保留 | 处理轨迹保留期 |
| D-45 | A5.2.1 AMX-504 | 用身份核对复核 | 自身身份需定期复核 |
| D-46 | A5.2.2 AMX-505 | 不清理 | ledger 不清理 |
| D-47 | A5.2.3 AMX-506 | v1 只处理 @ | 只过滤 @ |
| D-48 | A5.3.1 AMX-507 | 提供 | 失败筛选 |
| D-49 | A5.3.2 AMX-508 | 落盘 | 失败原文落盘 |
| D-50 | A5.3.3 AMX-509 | 提供「采用该身份」按钮 | 身份核对结果一键回填 |
| D-51 | A6.1.1 AMX-601 | 展示昵称/群名辅助识别 | 会话辅助标识 |
| D-52 | A6.1.2 AMX-602 | 保留记录并标记失效 | 失效会话不自动清理 |
| D-53 | A6.1.3 AMX-603 | v1 只做单条 | v1 不做批量作废 |
| D-54 | A6.2.1 AMX-604 | 展示并标注未回复原因 | 展示未回复消息 |
| D-55 | A6.2.2 AMX-605 | 做成适配器常量 | 机器人标记字段做成常量 |
| D-57 | A6.2.4 AMX-607 | 界面直白说明 | 上下文限制的界面说明 |
| D-58 | A7.1.1 AMX-701 | 默认关闭 | 自动压缩默认关闭 |
| D-60 | A7.1.3 AMX-703 | 需要二次确认 | 手动压缩需二次确认 |
| D-61 | A7.2.1 AMX-704 | v1 自动生效但可编辑 | 摘要自动生效但可编辑 |
| D-62 | A7.2.2 AMX-705 | v1 只留最新一版 | 摘要只留最新一版 |
| D-63 | A7.2.3 AMX-706 | 连续失败达阈值时自动关闭 | 连续压缩失败自动暂停 |
| D-64 | A8.1.1 AMX-801 | v1 只展示不校验 | 插件版本 v1 只展示 |
| D-65 | A8.1.2 AMX-802 | manifest 声明协议版本 | 协议版本协商 |
| D-66 | A8.1.3 AMX-803 | 尽量热生效 | 插件启停尽量热生效 |
| D-67 | A8.1.4 AMX-804 | 按声明能力分级 | 插件风险分级 |
| D-68 | A8.2.1 AMX-805 | 应用内页面 | 协议文档载体 |
| D-69 | A8.2.2 AMX-806 | v1 只支持一个根目录 | 只支持一个插件根目录 |
| D-70 | A8.2.3 AMX-807 | 5s | 插件握手超时 |
| D-71 | A9.1.1 AMX-901 | 支持 | 身份一键回填 |
| D-72 | A9.1.2 AMX-902 | 程序配置目录下的专用子目录 | 默认工作目录 |
| D-73 | A9.1.3 AMX-903 | 提供 60s / 120s / 300s 预设 | 超时分档预设 |
| D-74 | A9.1.4 AMX-904 | 按字符计数 | 字数按字符计 |
| D-77 | A9.2.1 AMX-907 | 分开放 | 数据库与归档分开放 |
| D-78 | A9.2.2 AMX-908 | 只导事件/台账/会话 | 不导入旧版日志 |
| D-79 | A9.2.3 AMX-909 | 不提供 | 不提供强制接管已有实例 |

---

## 四、后续动作

1. **D-23 需用户配合**：从另一钉钉账号给宿主发单聊消息，验证 conversation_id 格式
2. **D-33 需长期运行**：挂着观察 >300s 空闲后 bus 行为
3. **D-59/D-75 需长期使用**：随实际使用记录
4. **D-11 随视觉稿定**：三态文案与配色
5. **D-76 实现期确认**：逐项标注生效方式
6. 任何一条被推翻时：改对应功能的 `rules`（来源从 `自定义`/`代码实测` 改为 `待决` 或新来源）+ 回填本表，再跑 `coverage.js`
